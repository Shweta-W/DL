Step 1: Pull MySQL Image from Docker Hub
docker pull mysql:latest

Step 2: Verify Downloaded Images
docker images

Step 3: Run the MySQL Container
docker run --name test-mysql -e MYSQL_ROOT_PASSWORD=strong_password -d mysql

Step 4: Check Running Containers
docker ps

Step 5: Access the MySQL Container Terminal
docker exec -it test-mysql bash

Step 6: Connect to the MySQL Server
Inside the container terminal, run:
mysql -u root -p

Step 7: Execute SQL Commands

mysql> SHOW DATABASES;

Step 8: Exit MySQL and Container
exit   # exits MySQL shell
exit   # exits container shell


